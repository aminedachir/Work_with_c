#include <stdio.h>
#include <string.h>
char mot[50];
int s=0;
int main()
{
    scanf("%c",&mot);
    for(int i=0;i<strlen(mot);i++){
        char h[strlen(mot)] = ;
    }
    printf("%d",s);
    if (s == strlen(mot)){
        printf("match");
    }
}
